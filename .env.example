EXPO_PUBLIC_SUPABASE_URL=https://xxxx.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=colle-ici-la-cle-anon
# Copie ce fichier en .env et remplis les vraies valeurs (Supabase → Settings → API).
# La clé anon est publique par conception (protégée par les policies RLS), mais ce
# fichier reste hors de Git pour ne pas coupler le dépôt à un projet Supabase précis.
